<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Names</title>
    <style type="text/css">
        body {
            color: blanchedalmond;
            background-color: steelblue;
        }
    </style>
</head>

<body>
    <h1>At the Foo Bar!</h1>
    <h2>
        <%= foodInfo.name[0].toUpperCase() + foodInfo.name.slice(1, foodInfo.name.length) %>
    </h2>
    <img src="<%= foodInfo.image%>">
    <h3>Price: $<%= foodInfo.price %></h3>
    <p>
        <a href="<%= /foods/ %>">Back to Home</a>
    </p>


</body>

</html>