<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Drink Names</title>
    <style type="text/css">
        body {
            color: blanchedalmond;
            background-color: steelblue;
        }
    </style>
</head>

<body>
    <h1>Drinks!</h1>
    <ul>
        <% for (i=0; i < allDrinks.length; i++) { %>
            <li>
                <a href="/drinks/<%= [i] %>">
                    <%= allDrinks[i].name[0].toUpperCase() + allDrinks[i].name.slice(1, allDrinks[i].name.length) %>
                </a>
            </li>
            <% } %>
    </ul>
    <a href=" <%= /foods/ %>">Food Menu</a>
</body>

</html>